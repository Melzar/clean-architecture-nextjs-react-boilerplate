export type RoleAccounts = { admin: string; member: string };
export type ScreenIdentifiers = {
  login: string;
  dashboard: string;
  clients: string;
  clientDetails: string;
  packages: string;
};

export type ScreenRoutes = {
  clients: string;
  dashboard: string;
  packages: string;
};
